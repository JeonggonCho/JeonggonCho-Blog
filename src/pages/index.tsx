import React, { FC } from "react"
import Template from "../templates/Template"

const IndexPage: FC = () => {
  return (
    <Template>
      <div>메인 페이지</div>
    </Template>
  )
}

export default IndexPage